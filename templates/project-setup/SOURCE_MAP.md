# {PROJECT_NAME} Source Map

> 소스코드 위치를 빠르게 찾기 위한 라우팅 맵.
> 경로는 프로젝트 루트 기준 상대 경로.
>
> **작성 원칙**
> - 코드 위치를 찾을 때 가장 먼저 참조하는 문서다. 정확하지 않으면 의미가 없다 — **새 파일을 만들거나 옮기면 즉시 이 문서를 갱신한다.**
> - 모든 항목을 적지 않는다. 사람이 검색해서 못 찾을 만한 것, 디렉토리 이름만 봐서는 추측이 어려운 것 위주로 정리한다.
> - 표 한 줄에 한 항목. 설명은 한 줄을 넘지 않는다.

---

## Frontend (`{frontend-dir}/`)

### 페이지 라우트

| 라우트 | 파일 | 설명 |
|--------|------|------|
| `/` | `<path>` | <설명> |
| `/<route>` | `<path>` | <설명> |

### 레이아웃

| 파일 | 설명 |
|------|------|
| `<path>` | <설명> |

### 페이지 전용 컴포넌트

페이지와 같은 디렉토리에 두는 컴포넌트(co-location). 다른 페이지에서 재사용하지 않는다.

| 파일 | 설명 |
|------|------|
| `<path>` | <설명> |

### 공유 컴포넌트

| 경로 | 포함 컴포넌트 |
|------|--------------|
| `components/ui/atoms/` | <Button, Input, ...> |
| `components/ui/molecules/` | <SearchBar, Tabs, ...> |
| `components/ui/organisms/` | <Card, ...> |
| `components/feature/<domain>/` | <도메인별 컴포넌트> |
| `components/layout/` | <Header, Footer, ...> |

### API 클라이언트 (`lib/`)

| 파일 | 설명 |
|------|------|
| `<path>` | <설명> |

### 타입 정의 (`types/`)

| 파일 | 설명 |
|------|------|
| `<path>` | <설명> |

### 스타일 / 디자인 토큰

| 파일 | 설명 |
|------|------|
| `<path>` | <설명> |

---

## Backend (`{backend-dir}/`)

### 패키지 구조

```
{base-package}/
├── controller/        # HTTP 진입점
├── service/           # 비즈니스 로직
├── repository/        # 데이터 액세스
├── domain/ (or entity/) # 도메인 모델
├── dto/               # 요청/응답 DTO
├── config/            # Spring 설정
└── ...
```

### Controllers

| 경로/이름 | 설명 |
|-----------|------|
| `<path>` | <설명> |

### Services

| 경로/이름 | 설명 |
|-----------|------|
| `<path>` | <설명> |

### Repositories

| 경로/이름 | 설명 |
|-----------|------|
| `<path>` | <설명> |

### 도메인 모델 (Entities)

| 경로/이름 | 설명 |
|-----------|------|
| `<path>` | <설명> |

### 공통 계층 (Auth / Exception / Common)

| 경로/이름 | 설명 |
|-----------|------|
| `<path>` | <설명> |

### 마이그레이션 / DB 스크립트

| 경로 | 설명 |
|------|------|
| `<path>` | <Flyway/Liquibase 스크립트, 시드 데이터 등> |

> 스키마 자체는 [`DB_SCHEMA.md`](./DB_SCHEMA.md)를 참조.

---

## 인프라 / 설정 파일

| 파일 | 설명 |
|------|------|
| `<path>` | <설명> |

배포 관련 정보는 [`DEPLOY.md`](./DEPLOY.md)를 참조.
