interface:
  display_name: "Publish Agent Toolkit"
  short_description: "Commit, push main, reload Codex and Claude"
  default_prompt: "Use $publish-agent-toolkit to commit, push main, and reload agent-toolkit for Codex and Claude."
policy:
  allow_implicit_invocation: false
