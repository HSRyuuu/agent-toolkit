// =============================================================================
// schema.js — single IR shared by every page
//
// This file is regenerated by build.py from schema.dbml.
// If you are editing this by hand (LLM fallback path), keep the shape below.
//
// Shape:
//   window.SCHEMA = {
//     source_dbml: "<original dbml text>",
//     tables: [{
//       name, comment,
//       columns: [{
//         name, type, pk, uk, nullable, default, comment,
//         fk: { table, column } | undefined
//       }],
//       indexes: [{ name, columns: [...], unique }]
//     }],
//     relations: [{ from, from_col, to, to_col }],   // derived from FK
//     enums: [{ name, values: [...] }]
//   };
// =============================================================================

window.SCHEMA = {
  source_dbml: "",
  tables: [],
  relations: [],
  enums: []
};
